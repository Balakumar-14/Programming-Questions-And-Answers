#include<stdio.h> 
#include<string.h> 
int main()
{
char s[100], r[100]; int i,n,j=0; 
gets(s); 
n=strlen(s); 
for(i=0;i<n;i++) {

if(s[i]!='a'&&s[i] != 'e '&&s[i] != 'i'&&s[i]!='o'&&s[i] != 'u')

{
	r[j]=s[i];

j++;
}

}

printf("%s",r);

return 0;
}
