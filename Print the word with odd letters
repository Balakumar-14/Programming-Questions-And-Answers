#include<stdio.h>
int main()
{
	int i,j,n;
	char s[]="program";
	n=strlen(s);
	for(i=1;i<=strlen(s);i++)
	{
		for(j=1;j<=strlen(s);j++)
		{
			if(i==j||j==(n-i+1))
			{
				printf("%c",s[i-1]);
			}
			else
			{
				printf(" ");
			}
		}
		printf("\n");
	}
}
