#include<stdio.h>
int main()
{
	int n=2367,a,sum=0;
	while(n>0)
	{
		a=n%10;
		sum=sum*10+a;
		n=n/10;
	}
	while(sum>0)
	{
		a=sum%10;
		(a%2==0)?printf("%d-even\n",a):printf("%d-odd\n",a);
		sum=sum/10;
	}
}
